PUBLIC_API_URL=http://localhost:8787/api/v1

# Google reCAPTCHA v2 site key — leave unset to disable the captcha locally.
# The backend skips verification when RECAPTCHA_SECRET_KEY is not configured.
# PUBLIC_RECAPTCHA_SITE_KEY=
