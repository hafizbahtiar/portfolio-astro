# Taste
- Communicates in Malay (Bahasa Malaysia) mixed with English technical terms and expects replies in the same language/register. Confidence: 0.8
- Standardizes on bun as the package manager for the backend repo (`hono-workers`), keeping only `bun.lock` as canonical and treating a leftover `package-lock.json` as drift to remove. Confidence: 0.6
- Works on a product split across separate repos/sessions (frontend e.g. `portfolio-astro`, backend e.g. `hono-workers`) and expects the agent to handle the related repo directly - read files, run commands, and edit code in it - instead of only being handed instructions or a diff. Confidence: 0.6
- Reports bugs by naming the failing user action plus the path/directory of the screen involved, and expects the agent to trace the flow end-to-end across frontend and backend APIs to find the real root-cause layer rather than only the layer named in the report. Confidence: 0.6
- Flags problems by pasting raw terminal/console output verbatim with little or no instruction (e.g. a `DeprecationWarning` with its PID, or a full remote-CI build-failure log from an `npm ci` runner), expecting the agent to work out on its own whether it originates in the user's code or in a dependency, which dependency/path triggers it, and whether it is actually reproducible/reachable - not just a generic explanation. Confidence: 0.65
- After a fix, expects a proactive, comprehensive "check everything, what else is missing" audit of the whole integration surface (e.g. cross-checking every frontend API call against backend routes, method contracts, CSRF/CORS guards) rather than stopping at the reported bug; when findings are presented, actively asks for more ("point out lagi yang lain") and wants the audit kept going. Confidence: 0.75
- Wants work committed locally but never pushed/published without explicit approval - no push to main, no branch publication, no remote migrations or production data mutation unless explicitly requested. Confidence: 0.85
- Prefers staged, phase-by-phase implementation (small reviewable increments) over one large change, and will ask to "fix it step by step, phase by phase." Confidence: 0.8
- Treats production/remote data as off-limits during code work: wants idempotent, migration-safe SQL and code that works with the current schema; if a change cannot be safely applied, document it clearly instead of pretending it is done. Confidence: 0.85
- Expects detailed, explicit handoff/report artifacts (files under docs/reports, docs/plans) listing files changed, commands run and their results, what was done/skipped/blocked and why, and a suggested review checklist. Confidence: 0.8
- Wants strict, non-overclaiming status reporting (DONE / PARTIAL / BLOCKED / NOT DONE / INTENTIONALLY SKIPPED) and requires the agent to be explicit about anything only documented but not implemented. Confidence: 0.75
- Uses a second AI/tool (e.g. Claude) to review work afterward, so prioritizes clean structure, safe defaults, and easy review over speed. Confidence: 0.75
- Dislikes hardcoded data patches (e.g. hardcoded family member names in frontend merge logic) - prefers fixing root data issues and using stable identifiers over name-based matching. Confidence: 0.75
- Keeps changes scoped to the feature module and directly related docs; avoids unrelated redesigns, unnecessary dependency changes, and replacing working libraries unless proven impossible. Confidence: 0.8
- Prefers small pure, testable, framework-independent functions and removing duplicated logic (DRY). Confidence: 0.7
- Prefers server-side/SSR data fetching over client-side fetch waterfalls and cache-busting; wants existing backend/KV/CDN caching preserved. Confidence: 0.7
- Applies privacy-by-default minimization: restrict public payloads with strict whitelist-based DTO/sanitizers, strip notes/metadata/internal fields, and avoid exposing sensitive data (e.g. show year-only birth dates for living people). Confidence: 0.8
- Wants API clients to surface failures rather than mask them: reads may legitimately return null/empty on 404, but writes (POST/PUT/PATCH/DELETE) must throw so callers can never render a false "saved" success from a swallowed error. Confidence: 0.6
- Works through the audit findings incrementally by picking a subset - either by item number (e.g. "repair 1,2,3") or by priority tier (e.g. "proceed P1") - expecting only the selected items to be fixed in that round and the rest left for later, no blanket "fix everything at once". Confidence: 0.65
- Prefers security/privacy over convenience: will refuse to enable publicly-exposed or otherwise risky config (e.g. a public object-storage URL) just to make a feature work, defaults to private/least-privilege, and prefers removing or leaving a feature idle over shipping an insecure one. Confidence: 0.7
- Points the agent at another repo under `~/Developments` as the reference for established conventions/policies (e.g. storage security patterns) and expects it to read that codebase and mirror its patterns. Confidence: 0.6
- Prefers completing a half-built feature over deleting it: when told a UI element is "dead" because the backend is missing, will instead ask to build the missing backend (schema + service + route + dashboard wiring) so the existing frontend works, and expects the agent to restore/de-wire the code it removed rather than dropping the feature. Confidence: 0.6
- Has an appetite for continuous iterative enhancement: keeps asking "what else can we add?" and expects proactive proposals for the next concrete, scoped improvements rather than treating a feature as finished. Confidence: 0.55
- Maintains a living, priority-tagged audit doc per repo (e.g. `TODO.md`, P0 = bug/vuln, P1 = perf/correctness, P2 = polish/cleanup, P3 = later) and when asked to "update todo" expects the agent to read the existing file first, correct stale entries with the reason they changed, and keep the open items grouped by priority/section. Confidence: 0.7
- Wants that TODO doc to be a pure checklist of *remaining* work: completed items are deleted outright (their details live in the git diff/commit history, not the doc), not left ticked off as `[x]`, and finished/prose "Fixed & deployed" blocks are trimmed - only genuinely important warnings (e.g. a required migrate-then-deploy ordering, a `remote: true` caveat) survive as notes. Confidence: 0.65
- Expects heavy, repeated verification - explicitly asks to "buat semak ja banyak-banyak" (just do lots of checking) after changes, and will also just name a feature and ask to "verify pasal X" expecting a full proof pass rather than accepting the work at face value. Confidence: 0.7
- When a feature has an approved design spec/decision doc under `docs/`, treats it as the contract: checks the implementation against it, runs the verification the spec calls for (e.g. `npm run build`), and flags spec/decision-doc drift as a TODO item rather than editing the doc in passing. Once the feature has actually shipped, wants the spec status flipped to "Implemented" with a dedicated "Implementation notes" section listing each deliberate deviation from the spec (and companion handoff docs marked Done with what is still open). Confidence: 0.7
- When a field is genuinely dead (never incremented/read by anyone), prefers removing it from the type + API/mapper surface rather than keeping it or building it out, but refuses lossy/destructive schema migrations: leaves the DB column in place and annotates it as `-- unused` in the schema file instead of running `DROP COLUMN`. Confidence: 0.55
- Avoids persisting fields that no one reads (e.g. dropping a `referer` column in favour of a more accurate `source` value) - treats unused columns/fields as dead data to prevent up front. Confidence: 0.55
- Wants dead code deleted outright rather than left around or "wired up later": unused static assets (zero-reference `.svg` files) and unused public service exports get removed, along with any type/interface that only those dead exports referenced (e.g. an orphaned `ContactStats`), with references re-grepped first to confirm they are truly unused. Confidence: 0.55
- Expects the agent to verify behavior by actually running it locally (seeding sample data, starting the dev server, triggering the real handler - e.g. `wrangler dev --test-scheduled` + `curl /__scheduled` for a cron, or a real file upload read back byte-for-byte) rather than assuming it works, and to clean up after itself: remove seeded test rows and uploaded test objects, revert test users, kill background dev servers, and confirm no leftover processes. Confidence: 0.65
- Wants maintenance/scheduled work over visitor/PII data implemented properly (e.g. a retention window with scheduled pruning instead of keeping rows forever), run as a fault-tolerant background job (`ctx.waitUntil`, never throwing, logging failures) so a non-critical job error never becomes an incident. Confidence: 0.55
- Prefers a plain code-level constant over a config knob/toggle for internal tuning values (e.g. a 180-day retention window), to be edited in code only when the value needs to change. Confidence: 0.5
- Prefers the smallest viable fix that actually solves the measured problem over a bigger solution suggested for it: when a TODO floats a larger remedy (e.g. "combine the pills into a menu"), first quantify/estimate the real impact and pick the minimal change; treat the suggested approach as optional, not a mandate. Confidence: 0.55
- Treats client-side-only safeguards (e.g. a browser-side login lockout after 5 failed attempts) as UX sugar at most, and expects the real enforcement to live in the backend - asks the agent to confirm server-side rate limiting / throttle layers exist rather than trusting the frontend control. Confidence: 0.5
- Writes TODO/audit entries as diagnoses (file:line anchor plus a stated cause, e.g. "still `cache: 'no-store'`") but treats them as hypotheses: expects the agent to validate each entry against the actual code and fix only what is genuinely affected (e.g. skipping pages that turn out to be `prerender = true`, where the flagged issue has no runtime effect). Confidence: 0.5
- Kicks off work by asking the agent to first read and genuinely study the whole repo ("baca projek ni. fahamkan dan study") - map the stack, structure and conventions - before touching the selected snippet, and expects a concise grounded summary (what it is, data flow, relevant layers) rather than immediate edits. Confidence: 0.6
- Prioritizes accessibility over visual compactness: keeps text labels on controls even when space-constrained rather than switching to icon-only (unlabeled icons are a readability/accessibility regression), and preserves `aria-pressed`/`aria-label` for state. Confidence: 0.5
- Wants a feature verified as a full contract both ways, not by reading one side: walk the caller (URL join, method, payload, response handling) against the server (route mount, validator regex, response shape), run the repo's existing verification tooling for real (e.g. `bun scripts/verify-<feature>.ts`), then state plainly which layers could not be proven locally (e.g. browser UI when dev/build is broken) and list the exact prod prerequisites (migrations, secrets, webhook/route registration) before it can go live. Confidence: 0.55
