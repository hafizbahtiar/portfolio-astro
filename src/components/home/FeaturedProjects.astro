---
import type { Project } from "../../types/project";
import ProjectCard from "../project/ProjectCard.astro";

interface Props {
  projects: Project[];
}

const { projects } = Astro.props;
// 5 cards: the featured one spans both columns, the rest fill 2×2 — no holes.
const visible = projects.slice(0, 5);
---

<div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
  {
    visible.map((project, index) => (
      <div
        class={index === 0 ? "sm:col-span-2" : ""}
        data-reveal
        style={`--reveal-delay: ${(index % 2) * 80}ms`}
      >
        <ProjectCard
          title={project.title}
          description={project.description}
          imageUrl={project.imageUrl}
          imageVariant={project.imageVariant}
          imageLoading={index === 0 ? "eager" : "lazy"}
          imageFetchPriority={index === 0 ? "high" : "auto"}
          tags={
            project.technologies?.length
              ? project.technologies
              : (project.tags ?? [])
          }
          link={`/projects/${project.slug}`}
          year={project.year}
          projectType={project.projectType}
          featured={index === 0 && project.featured}
        />
      </div>
    ))
  }
</div>
