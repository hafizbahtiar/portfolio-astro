---
interface Props {
  label: string;
  name: string;
  value?: string;
  required?: boolean;
  class?: string;
  id?: string;
  placeholder?: string;
}

const {
  label,
  name,
  value = "",
  required = false,
  class: className = "",
  id: inputId = `${name}-input`,
  placeholder,
} = Astro.props;
---

<div class={`space-y-2 ${className}`}>
  <label
    class="block text-sm font-medium text-gray-500 dark:text-gray-400"
    for={inputId}
  >
    {label}
  </label>
  <input
    id={inputId}
    type="date"
    name={name}
    value={value}
    required={required}
    placeholder={placeholder}
    class="w-full bg-white dark:bg-gray-800 border border-gray-300 dark:border-gray-600 rounded-lg px-4 py-2 text-gray-900 dark:text-gray-100 focus:ring-2 focus:ring-sky-500/50 focus:border-sky-500 outline-none transition-all text-sm"
  />
</div>
